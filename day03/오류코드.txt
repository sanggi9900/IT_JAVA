
Some code changes cannot be hot swapped into a running virtual machine, such as changing method names or intrducing errors into running code.

The current target virtual machine [dat16.Quiz2 at localhost:49893] from launch [Quiz2 (2)] was unable to replace the running code with the code in the workspace

If us safe to contiune running the application, but you may notice discrepancies when debugging this application

Reason:
Hot code replace failed - Delete method not implemented


메서드 이름을 변경하거나 실행 중인 코드에 오류를 유발하는 등 일부 코드 변경 사항은 실행 중인 가상 머신으로 핫스왑될 수 없습니다.

[Quiz2 (2)] 시작 시 현재 대상 가상 머신 [localhost:49893의 dat16.Quiz2]가 실행 중인 코드를 작업 영역의 코드로 바꿀 수 없습니다.

애플리케이션을 계속 실행해도 안전하지만 이 애플리케이션을 디버깅할 때 불일치가 발견될 수 있습니다.

이유:
핫 코드 교체 실패 - 삭제 방법이 구현되지 않았습니다.